spring:
  application:
    name: emp-service
  datasource:
    url: jdbc:mysql://localhost:3307/training
    username: root
    password: root
  jpa:
    show_sql: true
server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
